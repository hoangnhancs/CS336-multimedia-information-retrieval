import numpy as np
result = np.array(['R', 'I', 'R', 'R', 'I', 'R'])

total = 5

#map = sum([])/5

#print(map)

#np.sum([result[:i]=='R', axis==0)/i for i in range (len(result)])

#a = [np.sum(result[:i]=='R', axis=0)] for i in range(6)
#print(a)